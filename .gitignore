/dist
/node_modules
/coverage
/test-results
/playwright-report
/stats.html
*log*
.history
*lock*
# *log* 会误伤 logo 命名的资源（examples/layout/logo.html、golden 快照 logo-*.png），显式放行
!**/logo*.html
!**/logo-*.png
# *log* 同样误伤 CHANGELOG.md（整个仓库因此一直没有变更日志），显式放行
!CHANGELOG.md
# *lock* 还会误伤 package-lock.json —— 它因此从未被跟踪，CI 每次安装都重新解析依赖、不可复现
# （与 CHANGELOG 的误伤同一类问题）。显式放行。
!package-lock.json
